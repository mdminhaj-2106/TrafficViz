import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

// Test: Re-enable App import to see if it loads
createRoot(document.getElementById("root")!).render(<App />);
